import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import MapScreenComponent from '@/components/Map/MapScreen'

const MapScreen = () => {
    return (
        <MapScreenComponent />
    )
}

export default MapScreen

const styles = StyleSheet.create({})